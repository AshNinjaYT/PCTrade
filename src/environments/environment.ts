export const environment = {
  // production en false indica que estamos trabajando en modo de desarrollo local
  production: false,
  
  // Guardamos la URL base de nuestro servidor (json-server) aquí.
  // De esta forma, si pasamos a producción, solo cambiamos este string
  // sin tener que buscar por todos los archivos del proyecto.
  apiUrl: 'http://localhost:3000/elements'
};
